<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.VibeVerse" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <item name="android:statusBarColor">@android:color/black</item> <!-- Set the status bar color to black -->
        <item name="android:windowLightStatusBar">false</item> <!-- This ensures icons/text are light-colored on the dark status bar -->
    </style>

    <style name="Theme.VibeVerse" parent="Base.Theme.VibeVerse" />
</resources>
